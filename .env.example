# Auth backend: supabase | neon (default: supabase)
VITE_AUTH_BACKEND=supabase

# Supabase (when VITE_AUTH_BACKEND=supabase)
VITE_SUPABASE_URL=https://your-project.supabase.co
VITE_SUPABASE_ANON_KEY=your-anon-key

# Neon (when VITE_AUTH_BACKEND=neon) - set in Netlify env, not in .env for frontend
# NEON_DATABASE_URL=postgresql://...
# AUTH_JWT_SECRET=your-secret-at-least-32-chars
